const CustomError = require("../extensions/custom-error");

class VigenereCipheringMachine {
  encrypt(arg1, arg2) {
    if (arg1 == undefined, arg2== undefined) throw new Error
  }    
  decrypt(arg1 , arg2) {
    if (arg1 == undefined, arg2== undefined) throw new Error
  }
}

module.exports = VigenereCipheringMachine;
